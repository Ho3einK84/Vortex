{
  "name": "vortex",
  "version": "1.3.1",
  "description": "Vortex — a bold Neo-Brutalist, self-contained subscription page template for the Rebecca panel.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "guard": "node scripts/build.mjs --guard-only",
    "serve": "node scripts/serve.mjs",
    "test:visual": "node tests/visual-regression.mjs"
  },
  "devDependencies": {
    "@unocss/core": "0.58.9",
    "@unocss/preset-uno": "0.58.9",
    "esbuild": "0.20.2"
  },
  "dependencies": {
    "qrcode-generator": "1.4.4"
  }
}
